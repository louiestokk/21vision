# public/

Place static assets here that should be served from the root URL.
To add your showreel video, place a file named `showreel.mp4` directly in this folder.
Vite will automatically expose it at `/showreel.mp4`.
